# Extra dependencies for packaging the desktop app
# (backend/build_desktop_app.sh on macOS, backend/build_desktop_app.ps1 on Windows).
# Not needed for normal web/dev use — install alongside requirements.txt only when building.
-r requirements.txt
pywebview==6.2.1
pyobjc-framework-Cocoa==12.0; sys_platform == "darwin"
# pywebview's Windows backend (platforms/winforms.py) embeds a WinForms window
# via .NET interop — pythonnet provides the `clr` module it imports.
pythonnet==3.0.3; sys_platform == "win32"
pyinstaller==6.22.3
